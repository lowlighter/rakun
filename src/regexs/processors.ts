/** 
 * Processors regexs
 */
  export default Object.freeze({

    //Post-processors
      post:{
        //Serie post-processors
          serie:{
            range:/^(\d+) (\d+)$/,
            single:/^(\d+)$/,
          },
      },
      
  })